<?xml version="1.0"?>
<project xmlns:ivy="antlib:org.apache.ivy.ant" name="UMB-CS680-2015F" default="dist" basedir=".">

	<property name="src.dir" value="src" />
	<property name="bin.dir" value="bin" />
	<property name="lib.dir" value="lib" />
	<property name="doc.dir" value="doc" />
	<property name="jar.dir" value="${bin.dir}/jar" />
	<property name="classes.dir" value="${bin.dir}/classes" />
	<property name="test.dir" value="${src.dir}/test" />
	<property name="report.dir" value="${bin.dir}/report" />
	<property name="junit.report.dir" value="${report.dir}/junit" />
	<property name="main-class-hw01" value="edu.umb.cs680.hw01.HelloWorld" />
	<property name="test-suit-hw01" value="edu.umb.cs680.hw01.HelloWorldTest" />

	<property name="ivy.install.version" value="2.4.0" />
	<condition property="ivy.home" value="${env.IVY_HOME}">
		<isset property="env.IVY_HOME" />
	</condition>
	<property name="ivy.home" value="${user.home}/.ant" />
	<property name="ivy.jar.dir" value="${ivy.home}/lib" />
	<property name="ivy.jar.file" value="${ivy.jar.dir}/ivy.jar" />

	<target name="download-ivy" unless="offline">
		<mkdir dir="${ivy.jar.dir}"/>
		<get src="https://repo1.maven.org/maven2/org/apache/ivy/ivy/${ivy.install.version}/ivy-${ivy.install.version}.jar" dest="${ivy.jar.file}" usetimestamp="true"/>
	</target>

	<target name="init-ivy" depends="download-ivy">
		<path id="ivy.lib.path">
			<fileset dir="${ivy.jar.dir}" includes="*.jar"/>
		</path>
		<taskdef resource="org/apache/ivy/ant/antlib.xml" uri="antlib:org.apache.ivy.ant" classpathref="ivy.lib.path"/>
	</target>

	<target name="resolve" depends="init-ivy">
		<ivy:retrieve />
		<ivy:cachepath pathid="compile.path" conf="compile" />
		<ivy:cachepath pathid="runtime.path" conf="runtime" />
		<ivy:cachepath pathid="test.path" conf="test" />
	</target>

	<path id="classpath.main">
		<pathelement location="${jar.dir}/${ant.project.name}.jar"/>
	</path>

	<path id="classpath.test">
		<pathelement location="${lib.dir}/junit-4.12.jar" />
		<pathelement location="${lib.dir}/hamcrest-core-1.3.jar" />
		<pathelement location="${jar.dir}/${ant.project.name}-test.jar"/>
	</path>

	<path id="classpath.lib">
		<fileset dir="${lib.dir}" includes="**/*.jar"/>
	</path>

	<target name="compile" depends="resolve">
		<mkdir dir="${classes.dir}" />
		<javac srcdir="${src.dir}" destdir="${classes.dir}" includeantruntime="false">
			<classpath>
				<path refid="classpath.lib" />
			</classpath>
		</javac>
	</target>

	<target name="jar" depends="compile">
		<mkdir dir="${jar.dir}"/>
		<jar destfile="${jar.dir}/${ant.project.name}.jar" basedir="${classes.dir}" excludes="**/*Test.class">
			<manifest>
				<attribute name="Main-Class" value="${main-class-hw01}"/>
			</manifest>
		</jar>
		<jar destfile="${jar.dir}/${ant.project.name}-test.jar" basedir="${classes.dir}">
			<manifest>
				<attribute name="Main-Class" value="${main-class-hw01}"/>
			</manifest>
		</jar>
	</target>

	<target name="run" depends="jar">
		<java fork="true" classname="${main-class-hw01}">
			<classpath>
				<path refid="classpath.lib" />
				<path refid="classpath.main" />
			</classpath>
		</java>
	</target>

	<target name="junit" depends="jar">
		<mkdir dir="${junit.report.dir}"/>
		<junit fork="true" printsummary="yes" haltonfailure="no" haltonerror="yes">
			<test name="${test-suit-hw01}" todir="${junit.report.dir}" />
			<classpath>
				<path refid="classpath.test"/>
			</classpath>
			<formatter type="xml"/>
			<batchtest todir="${junit.report.dir}">
				<fileset dir="${test.dir}" includes="*Test.java"/>
			</batchtest>
		</junit>
	</target>

	<target name="report" depends="junit">
		<junitreport todir="${junit.report.dir}">
			<fileset dir="${junit.report.dir}" includes="TEST-*.xml"/>
			<report todir="${junit.report.dir}"/>
		</junitreport>
	</target>

	<target name="dist" depends="clean, junit">
	</target>

	<target name="clean">
		<delete dir="${bin.dir}" />
		<delete dir="${lib.dir}" />
	</target>

	<target name="clean-all" depends="clean">
		<ivy:cleancache/>
	</target>

</project>
